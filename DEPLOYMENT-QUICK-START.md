# راهنمای سریع استقرار

این یک راهنمای سریع برای استقرار پروژه است. برای جزئیات بیشتر، فایل `DEPLOYMENT.md` را مطالعه کنید.

## 📦 فایل‌های ایجاد شده

### 1. فایل‌های پیکربندی

- **`env.production.template`**: الگوی فایل متغیرهای محیطی
- **`ecosystem.config.js`**: تنظیمات PM2 برای مدیریت process
- **`next.config.ts`**: بهینه‌سازی شده برای production

### 2. اسکریپت‌ها

- **`scripts/setup-db-production.js`**: راه‌اندازی خودکار دیتابیس
- **`scripts/check-deployment.js`**: بررسی آماده‌سازی برای استقرار
- **`scripts/health-check.js`**: بررسی سلامت سیستم

### 3. مستندات

- **`DEPLOYMENT.md`**: راهنمای کامل استقرار
- **`DEPLOYMENT-CPANEL.md`**: راهنمای استقرار روی cPanel (مخصوص هاست فریم‌ورک ایران)
- **`DEPLOYMENT-QUICK-START.md`**: این فایل (راهنمای سریع)

## 🚀 مراحل سریع

### 1. آماده‌سازی محلی

```bash
# کپی کردن فایل محیطی
cp env.production.template .env.production

# ویرایش .env.production و وارد کردن اطلاعات واقعی

# ساخت پروژه
pnpm build
```

### 2. آپلود به هاست

آپلود کنید:
- پوشه `.next/`
- پوشه `public/`
- فایل‌های: `package.json`, `next.config.ts`, `tsconfig.json`
- فایل `.env.production` (به عنوان `.env`)

### 3. راه‌اندازی در هاست

```bash
# نصب وابستگی‌ها
pnpm install --production

# راه‌اندازی دیتابیس
pnpm setup-db-production

# بررسی آماده‌سازی
pnpm check-deployment

# راه‌اندازی با PM2
pm2 start ecosystem.config.js
pm2 save
```

### 4. بررسی سلامت

```bash
# بررسی سلامت سیستم
pnpm health-check
```

## 📝 دستورات مفید

```bash
# بررسی آماده‌سازی
pnpm check-deployment

# راه‌اندازی دیتابیس
pnpm setup-db-production

# بررسی سلامت
pnpm health-check

# ساخت پروژه
pnpm build

# اجرای production
pnpm start
```

## ⚠️ نکات مهم

1. فایل `.env.production` را هرگز در Git commit نکنید
2. مطمئن شوید که تمام متغیرهای محیطی تنظیم شده‌اند
3. قبل از استقرار، `pnpm build` را اجرا کنید
4. پوشه `public/uploads` باید قابل نوشتن باشد

## 🔗 لینک‌های مفید

- راهنمای کامل: [DEPLOYMENT.md](./DEPLOYMENT.md)
- **راهنمای cPanel (توصیه می‌شود):** [DEPLOYMENT-CPANEL.md](./DEPLOYMENT-CPANEL.md)
- راهنمای زرین‌پال: [ZARINPAL_GUIDE.md](./ZARINPAL_GUIDE.md)
- راهنمای PostgreSQL: [README-POSTGRES.md](./README-POSTGRES.md)

