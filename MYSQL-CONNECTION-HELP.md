# 🔧 راهنمای رفع مشکل اتصال MySQL

## ⚠️ مشکل فعلی

اتصال به MySQL برقرار نمی‌شود. این می‌تواند به دلایل زیر باشد:

## 🔍 بررسی مشکلات

### 1. MySQL در حال اجرا نیست

**برای Windows:**
```powershell
# بررسی سرویس MySQL
Get-Service -Name "*mysql*"

# راه‌اندازی MySQL (اگر XAMPP استفاده می‌کنید)
# از XAMPP Control Panel استفاده کنید
```

**برای هاست cPanel:**
- MySQL معمولاً از قبل در حال اجرا است
- از cPanel → MySQL Databases بررسی کنید

### 2. Host اشتباه است

اگر از هاست cPanel استفاده می‌کنید، ممکن است نیاز باشد از host دیگری استفاده کنید:

```env
# برای هاست cPanel معمولاً localhost درست است
DB_HOST=localhost

# اما اگر کار نکرد، ممکن است نیاز به IP یا hostname خاص باشد
# از cPanel → MySQL Databases → Connection Strings بررسی کنید
```

### 3. پورت اشتباه است

بررسی کنید که پورت درست است:

```env
DB_PORT=3306  # برای MySQL
```

### 4. اطلاعات اتصال اشتباه است

از cPanel → MySQL Databases بررسی کنید:
- نام دیتابیس
- نام کاربر
- رمز عبور

## ✅ راه‌حل‌های پیشنهادی

### راه‌حل 1: استفاده از XAMPP (برای Windows محلی)

1. XAMPP را نصب کنید
2. MySQL را از XAMPP Control Panel راه‌اندازی کنید
3. فایل `.env` را تنظیم کنید:

```env
DB_HOST=localhost
DB_PORT=3306
DB_NAME=saded
DB_USER=root
DB_PASSWORD=  # خالی بگذارید یا رمز XAMPP
```

### راه‌حل 2: استفاده از هاست cPanel

اگر از هاست cPanel استفاده می‌کنید:

1. از cPanel → MySQL Databases بروید
2. اطلاعات دقیق را کپی کنید
3. در فایل `.env` وارد کنید

**نکته:** برای هاست cPanel، معمولاً:
- Host: `localhost`
- Port: `3306`
- Database: `username_dbname` (format cPanel)
- User: `username_dbuser` (format cPanel)

### راه‌حل 3: تست اتصال دستی

```bash
# تست اتصال با mysql command line
mysql -h localhost -P 3306 -u shop1111_saded002 -p shop1111_saded02
```

اگر این کار کرد، مشکل از کد نیست.

## 🚀 پس از رفع مشکل

پس از اتصال موفق:

```bash
# راه‌اندازی جداول
npm run setup-mysql

# تست اتصال
node scripts/test-db-connection.js

# اجرای پروژه
npm run dev
```

## 📞 نیاز به کمک؟

اگر مشکل ادامه داشت:
1. خروجی `node scripts/test-db-connection.js` را بررسی کنید
2. از cPanel → MySQL Databases → Connection Strings استفاده کنید
3. با پشتیبانی هاست تماس بگیرید

