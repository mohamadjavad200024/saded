# راهنمای کامل راه‌اندازی درگاه پرداخت زرین‌پال

## 📋 مراحل راه‌اندازی

### مرحله 1: ثبت‌نام در زرین‌پال

1. به وب‌سایت [زرین‌پال](https://www.zarinpal.com) بروید
2. روی دکمه "ثبت‌نام" کلیک کنید
3. فرم ثبت‌نام را پر کنید:
   - نام و نام خانوادگی
   - شماره موبایل
   - ایمیل
   - کد ملی
   - رمز عبور
4. ایمیل خود را تایید کنید

### مرحله 2: تکمیل احراز هویت

1. وارد پنل کاربری زرین‌پال شوید
2. به بخش "احراز هویت" بروید
3. اطلاعات زیر را وارد کنید:
   - اطلاعات هویتی
   - اطلاعات بانکی (شماره حساب و شبا)
   - اطلاعات تماس
4. مدارک مورد نیاز را آپلود کنید
5. منتظر تایید زرین‌پال بمانید (معمولاً 1-3 روز کاری)

### مرحله 3: دریافت Merchant ID

1. پس از تایید احراز هویت، به بخش "درگاه پرداخت" بروید
2. روی "ایجاد درگاه جدید" کلیک کنید
3. اطلاعات درگاه را وارد کنید:
   - نام درگاه
   - آدرس وب‌سایت
   - Callback URL (آدرس بازگشت از پرداخت)
4. Merchant ID خود را کپی کنید (فرمت: `xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx`)

### مرحله 4: تنظیم متغیرهای محیطی

1. در ریشه پروژه، فایل `.env.local` ایجاد کنید (اگر وجود ندارد)
2. متغیرهای زیر را اضافه کنید:

```env
# Merchant ID زرین‌پال (از پنل زرین‌پال کپی کنید)
ZARINPAL_MERCHANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# URL پایه سایت (برای callback)
# در حالت development:
NEXT_PUBLIC_URL=http://localhost:3000

# در حالت production:
# NEXT_PUBLIC_URL=https://yourdomain.com
```

**⚠️ نکته مهم:** فایل `.env.local` را به `.gitignore` اضافه کنید تا در Git commit نشود!

### مرحله 5: تست در حالت Sandbox

برای تست قبل از استفاده واقعی:

1. از Merchant ID تست استفاده کنید:
   - برای دریافت Merchant ID تست، به [صفحه تست زرین‌پال](https://sandbox.zarinpal.com) بروید
   - یا از Merchant ID تست استفاده کنید: `xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx`

2. در `.env.local`:
```env
ZARINPAL_MERCHANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
NEXT_PUBLIC_URL=http://localhost:3000
NODE_ENV=development
```

3. شماره کارت تست:
   - شماره کارت: `6037-9971-1234-5678`
   - CVV2: `123`
   - رمز دوم: `123456`
   - تاریخ انقضا: هر تاریخ آینده

### مرحله 6: راه‌اندازی در Production

1. در `.env.local` (یا متغیرهای محیطی سرور):
```env
ZARINPAL_MERCHANT_ID=your-real-merchant-id
NEXT_PUBLIC_URL=https://yourdomain.com
NODE_ENV=production
```

2. مطمئن شوید که Callback URL در پنل زرین‌پال به درستی تنظیم شده:
   ```
   https://yourdomain.com/payment/verify
   ```

## 🔧 تنظیمات فنی

### Callback URL

Callback URL آدرسی است که کاربر پس از پرداخت به آن هدایت می‌شود. باید:

1. در پنل زرین‌پال تنظیم شود
2. با آدرس در کد شما یکسان باشد
3. قابل دسترسی از اینترنت باشد (نه localhost در production)

### حالت Sandbox vs Production

- **Sandbox**: برای تست - از `sandbox.zarinpal.com` استفاده می‌کند
- **Production**: برای استفاده واقعی - از `api.zarinpal.com` استفاده می‌کند

سیستم به صورت خودکار بر اساس `NODE_ENV` تشخیص می‌دهد.

## 📝 کدهای خطا

| کد | معنی |
|---|---|
| 100 | موفق |
| -9 | خطای اعتبارسنجی |
| -10 | خطای پارامترها |
| -11 | Merchant ID نامعتبر |
| -12 | درخواست نامعتبر |
| -21 | IP نامعتبر |

## 🧪 تست

1. پروژه را اجرا کنید: `pnpm dev`
2. یک محصول به سبد خرید اضافه کنید
3. به صفحه Checkout بروید
4. اطلاعات را وارد کنید
5. روی "ادامه به درگاه پرداخت" کلیک کنید
6. در صفحه زرین‌پال، از شماره کارت تست استفاده کنید
7. پس از پرداخت، به صفحه verify هدایت می‌شوید

## 🔒 امنیت

- ✅ Merchant ID را هرگز در کد frontend قرار ندهید
- ✅ تمام درخواست‌های پرداخت از طریق API routes انجام می‌شود
- ✅ همیشه پرداخت را در سمت سرور verify کنید
- ✅ از HTTPS در production استفاده کنید

## 📞 پشتیبانی

- مستندات زرین‌پال: https://docs.zarinpal.com
- پشتیبانی زرین‌پال: https://www.zarinpal.com/contact
- تلفن پشتیبانی: 021-41239





