# 🔀 مقایسه: Git Version Control در cPanel vs Terminal

## ❓ سوال: آیا باید از Git Version Control در cPanel استفاده کنم؟

**پاسخ کوتاه:** **خیر، لازم نیست!** شما می‌توانید از Terminal استفاده کنید که ساده‌تر و سریع‌تر است.

---

## 🔀 دو روش برای دریافت پروژه

### روش 1: استفاده از Terminal (SSH) ✅ **توصیه می‌شود**

**مزایا:**
- ✅ سریع‌تر و مستقیم‌تر
- ✅ کنترل بیشتر
- ✅ دسترسی به تمام دستورات Git
- ✅ امکان استفاده از npm، PM2 و سایر ابزارها

**دستورات:**
```bash
cd ~/public_html
git clone https://github.com/mohamadjavad200024/saded.git
cd saded
npm install
# و بقیه مراحل...
```

---

### روش 2: استفاده از Git Version Control در cPanel

**مزایا:**
- ✅ رابط گرافیکی
- ✅ مدیریت از طریق cPanel
- ✅ امکان Pull خودکار

**معایب:**
- ❌ محدودیت در دستورات
- ❌ نیاز به تنظیمات بیشتر
- ❌ ممکن است با npm و PM2 مشکل داشته باشد

---

## 🎯 توصیه: استفاده از Terminal

از آنجایی که شما:
1. ✅ به Terminal دسترسی دارید
2. ✅ نیاز به npm install دارید
3. ✅ نیاز به PM2 دارید
4. ✅ نیاز به اجرای اسکریپت‌ها دارید

**بهترین روش: استفاده از Terminal است.**

---

## 📋 مراحل با Terminal (روش توصیه شده)

```bash
# 1. Clone کردن پروژه
cd ~/public_html
git clone https://github.com/mohamadjavad200024/saded.git
cd saded

# 2. نصب dependencies
npm install

# 3. ایجاد فایل .env
nano .env

# 4. راه‌اندازی دیتابیس
npm run setup-mysql

# 5. Build
npm run build

# 6. راه‌اندازی با PM2
pm2 start ecosystem.config.js
pm2 save
```

---

## 🔧 اگر می‌خواهید از Git Version Control در cPanel استفاده کنید

### تنظیمات در cPanel:

1. **از Git Version Control → Create** بروید
2. **Repository Name:** `saded`
3. **Repository Path:** `/home/shop1111/public_html/saded`
4. **Clone URL:** `https://github.com/mohamadjavad200024/saded.git`
5. روی **Create** کلیک کنید

**نکته:** پس از ایجاد، باید از Terminal برای نصب dependencies و راه‌اندازی استفاده کنید.

---

## ✅ نتیجه‌گیری

**برای پروژه شما، استفاده از Terminal بهتر است** چون:
- نیاز به npm install دارید
- نیاز به اجرای اسکریپت‌ها دارید
- نیاز به PM2 دارید
- کنترل بیشتری دارید

**Git Version Control در cPanel** بیشتر برای:
- پروژه‌های ساده
- زمانی که فقط می‌خواهید فایل‌ها را pull کنید
- مدیریت از طریق رابط گرافیکی

---

## 🚀 ادامه با Terminal

اگر از Terminal استفاده می‌کنید (که توصیه می‌شود)، ادامه دهید با:

```bash
cd ~/public_html/saded
npm install
```

**موفق باشید! 🎉**

