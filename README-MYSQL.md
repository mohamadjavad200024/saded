# راهنمای نصب و راه‌اندازی MySQL

## 🎯 مزایای MySQL

- ✅ **سازگاری بالا** - پشتیبانی از اکثر هاست‌ها و cPanel
- ✅ **سریع و ساده** - راه‌اندازی آسان
- ✅ **قدرتمند** - مناسب برای پروژه‌های بزرگ
- ✅ **قابل نگهداری** - راه‌اندازی و نگهداری آسان

## 🚀 روش سریع (توصیه می‌شود)

فقط یک دستور اجرا کنید:

```bash
pnpm setup-mysql
```

این اسکریپت به صورت خودکار:
- ✅ بررسی می‌کند که MySQL در دسترس است یا نه
- ✅ دیتابیس 'saded' را ایجاد می‌کند
- ✅ جداول را ایجاد می‌کند
- ✅ Index ها را ایجاد می‌کند

## 📋 پیش‌نیازها

### نصب MySQL

1. **برای هاست cPanel:**
   - MySQL معمولاً از قبل نصب است
   - می‌توانید از cPanel → MySQL Databases استفاده کنید

2. **برای سرور محلی:**
   - Windows: https://dev.mysql.com/downloads/installer/
   - Mac: `brew install mysql`
   - Linux: `sudo apt-get install mysql-server` (Ubuntu/Debian)

3. **بررسی نصب:**
   ```bash
   mysql --version
   ```

## ⚙️ تنظیمات دیتابیس

پس از نصب MySQL، دیتابیس با مشخصات زیر ایجاد می‌شود:

- **نام دیتابیس:** `saded`
- **کاربر:** `root` (یا کاربری که در .env تنظیم کرده‌اید)
- **رمز:** (رمز تنظیم شده در .env)
- **Host:** `localhost` (یا آدرس هاست)
- **Port:** `3306`

### تنظیم فایل .env

فایل `.env` را در ریشه پروژه ایجاد کنید:

```env
DB_HOST=localhost
DB_PORT=3306
DB_NAME=saded
DB_USER=root
DB_PASSWORD=your_password
```

### برای هاست cPanel

1. از cPanel → MySQL Databases بروید
2. یک دیتابیس جدید ایجاد کنید (مثلاً `username_saded`)
3. یک کاربر MySQL ایجاد کنید
4. کاربر را به دیتابیس اضافه کنید
5. اطلاعات را در فایل `.env` وارد کنید:

```env
DB_HOST=localhost
DB_PORT=3306
DB_NAME=username_saded
DB_USER=username_dbuser
DB_PASSWORD=your_password
```

## 🔧 راه‌اندازی دستی

اگر می‌خواهید به صورت دستی راه‌اندازی کنید:

```bash
# 1. اتصال به MySQL
mysql -u root -p

# 2. ایجاد دیتابیس
CREATE DATABASE IF NOT EXISTS saded CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 3. خروج
exit;

# 4. اجرای اسکریپت setup
pnpm setup-mysql
```

## ✅ بررسی اتصال

برای بررسی اتصال به دیتابیس:

```bash
node scripts/test-db-connection.js
```

یا:

```bash
pnpm health-check
```

## 🐛 رفع مشکلات

### خطای اتصال

اگر خطای اتصال دریافت کردید:

1. بررسی کنید که MySQL در حال اجرا است:
   ```bash
   # Windows
   net start MySQL
   
   # Linux/Mac
   sudo systemctl status mysql
   ```

2. بررسی کنید که اطلاعات در `.env` صحیح است

3. بررسی کنید که کاربر دسترسی به دیتابیس دارد

### خطای رمز عبور

اگر خطای رمز عبور دریافت کردید:

1. رمز را در فایل `.env` بررسی کنید
2. از cPanel → MySQL Databases → Modify User برای تغییر رمز استفاده کنید

### خطای دیتابیس وجود ندارد

اگر دیتابیس وجود ندارد:

```bash
pnpm setup-mysql
```

این دستور دیتابیس را ایجاد می‌کند.

## 📊 ساختار جداول

پس از راه‌اندازی، جداول زیر ایجاد می‌شوند:

- `products` - محصولات
- `categories` - دسته‌بندی‌ها
- `orders` - سفارشات
- `users` - کاربران
- `carts` - سبد خرید
- `quick_buy_chats` - چت‌های خرید سریع
- `chat_messages` - پیام‌های چت
- `chat_attachments` - فایل‌های پیوست چت
- `admin_presence` - وضعیت آنلاین ادمین

## 🎯 همه چیز آماده است!

پروژه شما حالا از MySQL استفاده می‌کند. 🚀

