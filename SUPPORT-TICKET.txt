موضوع: مشکل Resource temporarily unavailable - عدم امکان اجرای دستورات در ترمینال

سلام و وقت بخیر

با مشکل جدی در هاست مواجه شده‌ام و نیاز به کمک شما دارم.

🔴 مشکل:
در ترمینال SSH نمی‌توانم هیچ دستوری را اجرا کنم و خطای زیر را دریافت می‌کنم:
"bash: fork: retry: Resource temporarily unavailable"
"bash: fork: Resource temporarily unavailable"

این خطا برای تمام دستورات رخ می‌دهد، حتی دستورات ساده مانند:
- git fetch
- git pull
- pm2 restart
- pm2 status
- npm install
- و هر دستور دیگری که نیاز به fork دارد

📋 اطلاعات فنی:
- هاست: CloudLinux (بر اساس خطای cagefs_enter)
- کاربر: shop1111
- مسیر پروژه: /home/shop1111/public_html/saded
- Node.js: /opt/alt/alt-nodejs20/root/usr/bin/node
- PM2: ~/.npm-global/bin/pm2
- پروژه: Next.js با PM2

🔧 کارهایی که انجام داده‌ام:
1. تمام فایل‌های اسکریپت .sh که git fetch/pull داشتند را حذف کردم
2. از Cron Jobs برای restart PM2 استفاده کردم (اما فایده نداشت)
3. چند ساعت صبر کردم اما مشکل ادامه دارد
4. سعی کردم از cPanel File Manager استفاده کنم

❓ درخواست‌های من:
1. بررسی محدودیت‌های LVE (PMEM, Process Limits, CPU)
2. بررسی پروسه‌های در حال اجرا - آیا پروسه‌های اضافی Node.js یا PM2 وجود دارد؟
3. Kill کردن پروسه‌های اضافی (اگر وجود دارد)
4. Restart کردن PM2 برای من (نام پروژه: saded)
5. بررسی اینکه آیا محدودیت‌های هاست به درستی تنظیم شده است
6. اگر نیاز است، افزایش محدودیت‌های LVE

📝 اطلاعات اضافی:
- پروژه با PM2 در حال اجرا است (ecosystem.config.js)
- قبلاً همه چیز به درستی کار می‌کرد
- مشکل بعد از اجرای چندین دستور git fetch/pull شروع شد
- الان نمی‌توانم PM2 را restart کنم تا تغییرات جدید را اعمال کنم

⚠️ فوری:
اگر امکان دارد، لطفاً PM2 را برای من restart کنید:
/opt/alt/alt-nodejs20/root/usr/bin/node /home/shop1111/.npm-global/bin/pm2 restart saded --update-env

با تشکر از همکاری شما
شماره تیکت: [شماره تیکت را اینجا وارد کنید]

